---
import content from '@app/styles/content.module.css';
import PageLayout from '@app/layouts/PageLayout.astro';
import Stage from '@app/pages/stage/_islands/Stage2b';
import type { PageMetadata } from '@app/config/pages';
import { codeToHtml } from 'shiki';
import { PROJECT_REPO_URL } from '@app/config/repo';

export const metadata: PageMetadata = {
  title: 'Разные высоты у секторов',
  groupTitle: 'Stage 2: Отрисовка секторов',
};

const code1 = `  interface Sector {
    segs: Seg[];
    height: number;
  }`;

const code2 = `  function createCircleLines(
    centerX: number,
    centerY: number,
    radius: number,
    segments: number = 36,
  ): Seg[] {
    // ..
  }

  const sector1 = {
    height: 10_000,
    segs: createCircleLines(150, 130, 85, 8)
  };

  const sector2 = {
    height: 7_500,
    segs: createCircleLines(150, 130, 10, 10)
  };

  const sector3 = {
    height: 5_000,
    segs: createCircleLines(150, 130, 15, 10)
  };

  const sector4 = {
    height: 2_500,
    segs: createCircleLines(150, 130, 20, 10)
  };

  const level: Level = {
    linedefs: [
      ...sector1.segs, 
      ...sector2.segs,
      ...sector3.segs, 
      ...sector4.segs,
    ],
    sectors: [sector1, sector2, sector3, sector4]
  };`;

const code1Html = await codeToHtml(code1, { lang: 'ts', theme: 'github-light' });
const code2Html = await codeToHtml(code2, { lang: 'ts', theme: 'github-light' });
---

<PageLayout>
  <section class={content.contentSection}>
    <p class={content.paragraph}>
      Добавим в описание сектора высоту. Пусть стены сектора будут использовать одно и то же значение при построении проекции:
    </p>
    <div set:html={code1Html} />
    <p class={content.paragraph}>
      Затем, используя ранее созданный отрисовщик, попробуем сделать проекцию уровеня:
    </p>
    <Stage part={0} client:load />
    <p class={content.paragraph}>
      Для примера выше используем следующее описание уровня:
    </p>
    <div set:html={code2Html} />
    <p class={content.paragraph}>
      В описании уровня мы продолжаем хранить <code class={content.inlineCode}>level.linedefs</code>. Это необходимо для отрисовки карты,  2D-уровня, вид сверху.
    </p>
    <p class={content.paragraph}>
      <a href={`${PROJECT_REPO_URL}/stages/Stage2a/render25d.ts`} target="_blank" rel="noopener noreferrer" class={content.referenceLink} >Реализация шага на github</a>
    </p>
  </section>
</PageLayout>
